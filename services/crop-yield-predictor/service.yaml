apiVersion: v1
kind: Service
metadata:
  name: crop-yield-predictor
  namespace: production
spec:
  selector:
    app: crop-yield-predictor
  ports:
    - port: 80
      targetPort: 8080
  type: LoadBalancer
